#include <iostream>
#include <vector>
#include <unordered_map>

using namespace std;

void canBalancePotions(int t) {
    while (t--) {
        int n;
        cin >> n;
        vector<int> a(n);
        for (int i = 0; i < n; ++i) {
            cin >> a[i];
        }

        
}

int main() {
    int t;
    cin >> t;
    canBalancePotions(t);
    return 0;
}
